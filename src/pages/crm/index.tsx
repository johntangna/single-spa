import { h } from "../.."
import { ConnectToRedux } from "../connectToRedux"

type Props = {

} & App.ReduxProps

function CRM(props: Props) {
  return (
    <div>1235</div>
  )
}

export default ConnectToRedux(CRM)
